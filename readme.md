stp2step
========

Python progrm which must be run via `freecadcmd` to convert STL files to STEP files

Note: `squashfs-root` can be created with a command similar to `FreeCAD_weekly-builds-38495-conda-Linux-x86_64-py311.AppImage --appimage-extract`